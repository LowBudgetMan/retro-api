spring:
  application:
    name: retro-api
  data:
    jpa:
      repositories:
        enabled: true
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    show-sql: false
  liquibase:
    change-log: db/changelog.xml
  threads:
    virtual:
      enabled: true
  h2:
    console:
      enabled: false
      path: /h2
  datasource:
    url: jdbc:postgresql://db:5432/retroapi
    username: root
    password: retroapi
    driver-class-name: org.postgresql.Driver
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://auth-server:8080/realms/myrealm
web:
  authentication:
    authority: http://localhost:${KEYCLOAK_PORT:8010}/realms/myrealm
    clientId: retroquest-web
  cors:
    allowed-origins:
      - http://localhost:${UI_PORT:3000}
broker:
  relay:
    relay-host: rabbitmq
    relay-port: 61613
    relay-username: guest
    relay-password: guest
websocket:
  base-url: ws://localhost:${API_PORT:8080}
jwt:
  issuer-overrides:
    "http://localhost:${KEYCLOAK_PORT:8010}": "http://auth-server:8080"
